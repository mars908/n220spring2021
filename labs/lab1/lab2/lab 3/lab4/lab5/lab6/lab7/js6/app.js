let txtName = document.getElementById("txtName");
//This line takes the input from the user that is gotten from the html and makes it a js variable


function sayHello() {
    console.log("Hello " + txtName.value);
}
//This function simply displays the text of hello with whatever the user typed into the box.